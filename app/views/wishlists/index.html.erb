<div class='container'>
  <h1>Mes wishlists</h1>
  <!-- Button trigger modal -->
  <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalLong">
    Add a wishlist
  </button>

  <!-- Modal -->
  <div class="modal fade" id="exampleModalLong" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLongTitle">Add a wishlist</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <%= simple_form_for @wishlist do |f| %>
            <%= f.input :name %>
            <%= f.submit class:'btn btn-primary'%>
          <% end %>
        </div>
      </div>
    </div>
  </div><br>
  <% @wishlists.each do |wishlist| %>
    <h3><%= wishlist.name %> <%= link_to 'delete', wishlist_path(wishlist), method: :delete %> </h3>
      <% if !wishlist.places.nil? %>
        <%  wishlist.places.each do |place|%>
          <p><%= place.name %></p>
          <%= link_to 'daaaaete', place_wishlists_place_path(place, wishlist.wishlists_places.first), method: :delete %>
        <% end %>
      <% end %>
  <% end %>
</div>