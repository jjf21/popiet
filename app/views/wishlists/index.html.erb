<div class='container'>
  <h1>Mes wishlists</h1>
  <!-- Button trigger modal -->
  <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalLong">
    Add a wishlist
  </button>

  <!-- Modal -->
  <div class="modal fade" id="exampleModalLong" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLongTitle">Add a wishlist</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <%= simple_form_for @wishlist do |f| %>
            <%= f.input :name %>
            <%= f.submit class:'btn btn-primary'%>
          <% end %>
        </div>
      </div>
    </div>
  </div><br>
  <% @wishlists.each do |wishlist| %>
    <h3><%= wishlist.name %><h3><br>
      <% if !wishlist.wishlists_places.nil? %>
        <%  wishlist.wishlists_places.each do |wish_place|%>
          <%= wish_place.place.name %><br>
        <% end %>
      <% end %>
  <% end %>
</div>